"""
Mini-Program 2: String Methods
Topic: Strings

Learning Objectives:
- Use built-in string methods
- Transform strings (upper, lower, title, etc.)
- Search within strings
- Replace and split strings

Instructions:
Complete this program to practice using string methods.
"""

# Student Self-Check
# Run: python -m unittest starter_tests.test_05_strings
# - Confirm the text prints as uppercase and title case, and the phrase strips to "Hello World".
# - Confirm text starts with "python", ends with "ing", and finds "programming" at index 7.
# - Confirm replacement, splitting, joining, and counting "o" all produce the expected results.

# TODO 1-8: Practice common string transformations and searches
# Create the sample strings, transform their case, trim whitespace,
# test prefixes/suffixes, find text, and replace text.
# Hint: text.upper(), text.title(), phrase.strip(), text.startswith(...), text.endswith(...), text.find(...), text.replace(...)
# Write your code here:


# TODO 9-11: Split, join, and count text
# Turn a comma-separated string into a list, join a word list into one sentence,
# and count a chosen character in the sample text.
# Hint: sentence.split(","), " ".join(words), text.count("o")
# Write your code here:


# BONUS TODO: Compare multiple case conversions
# Start with the mixed-case phrase and print several case-transformed versions of it.
# Write your code here:
